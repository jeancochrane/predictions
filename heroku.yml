setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./scripts/release.sh
  image: web
run:
  web: daphne -t 180 --verbosity 2 predictions.asgi:application
